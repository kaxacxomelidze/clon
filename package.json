{
  "name": "clonyfy",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm install --prefix packages/cloner && npm run build --prefix packages/cloner",
    "ui": "node server.js",
    "start": "node server.js",
    "test": "npm test --prefix packages/cloner",
    "landing:build": "npm install --prefix landing && npm run build --prefix landing",
    "landing:dev": "npm install --prefix landing && npm run dev --prefix landing"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "archiver": "^7.0.1",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "nodemailer": "^8.0.7",
    "stripe": "^22.1.1"
  }
}
